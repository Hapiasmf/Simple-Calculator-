#include<bits/stdc++.h>
using name space std;
int nain()
{
  int n.m;
  cin >> n >>
  cout << "The number are:" << n >> " " << m >> end1;

  cout << "sum" << (n + m)<< end1;
  cout << "difference" << (n - m) << end1;
  cout << "product" << (n * m) << end1;
  cout << "quotient" << (n / m)<< end1;
  cout << "reminder" << (n § m) << end1;
  return 0;
}
